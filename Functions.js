/*var input = require('readline-sync');
var name = input.question("What is your name: ");
function greeting (nameString)
{
    console.log("Hello, " + nameString);
}

function Dismissal (nameString)
{
    console.log("Goodbye, " + nameString);
}

greeting(name);*/

//Proving that after return, the num
function multiply(firstNum, secondNum)
{
    return totalNum = firstNum * secondNum;
    totalNum = firstNum % secondNum;
}

multiply(5, 4);
console.log(totalNum);